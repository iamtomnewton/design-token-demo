import { DefaultTheme as Theme } from "../../../tokens/dist/styles";

//! DO NOT IMPORT FROM THIS FILE - USE './theme-selectors.ts' INSTEAD

export const getGroupOneSpacing = (theme: Theme) => {
  return theme.spacing.group.one;
};

export const getGroupTwoSpacing = (theme: Theme) => {
  return theme.spacing.group.two;
};

export const getGroupThreeSpacing = (theme: Theme) => {
  return theme.spacing.group.three;
};

export const getGroupFourSpacing = (theme: Theme) => {
  return theme.spacing.group.four;
};

export const getGroupFiveSpacing = (theme: Theme) => {
  return theme.spacing.group.five;
};

export const getDistinctOneSpacing = (theme: Theme) => {
  return theme.spacing.distinct.one;
};

export const getDistinctTwoSpacing = (theme: Theme) => {
  return theme.spacing.distinct.two;
};

export const getRelatedOneSpacing = (theme: Theme) => {
  return theme.spacing.related.one;
};

export const getRelatedTwoSpacing = (theme: Theme) => {
  return theme.spacing.related.two;
};
